# Mission

  Gitboard provides a local dashboard for developers to monitor and manage their workflow across GitHub and GitLab. It centralizes visibility into CI status, open merge requests/pull requests, failed job triage, and local checkout/worktree states using existing CLI tools (`gh`, `glab`, and `git`).