# Mission

Gitboard provides a local, interactive dashboard for developers to monitor GitLab and GitHub activity. It centralizes CI status, open Merge Requests/Pull Requests, and failed job triage into a single view. By integrating with local `gh`, `glab`, and `git` tools, it bridges the gap between remote forge data and the user's local development environment (worktrees and checkouts).