# Gitboard

This context branch contains the project understanding and architectural digest for the `gitboard` repository.

## Context Branch Purpose
This branch serves as the source of truth for the current project state, providing the "teaching story" for humans and grounding for agents. 

- **Seed Origin**: Derived from the `gitboard` repository (source_sha: 604a2371).
- **Usage**: Do not merge these files into the default branch. Context updates should be submitted as PRs targeting this branch.

## Project Overview
Gitboard is a local code-change board for GitLab and GitHub, providing visibility into CI status, open MRs/PRs, and local worktree states using `gh`, `glab`, and `git`.