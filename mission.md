# Mission

  Gitboard provides a local code-change dashboard for GitLab and GitHub. It enables developers to monitor CI status, open MRs/PRs, failed job triage, and local worktree states directly from their machine using `gh`, `glab`, and `git`.

  The system automates the discovery of repositories and provides AI-assisted triage to streamline developer workflows and branch maintenance.