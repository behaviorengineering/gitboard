# Gitboard (Context Branch)

  This context branch holds the project understanding for the `gitboard` repository, seeded from source SHA `5fca41ab298d424396b33e6790543f78220565a7`.

  ## Project Overview
  Gitboard is a local code-change board for GitLab and GitHub. It provides a dashboard for CI status, open MRs/PRs, failed job triage, and local checkout/worktree state using local forge CLIs (`gh`, `glab`) and `git`.

  ## Branch Purpose
  This branch is an orphan used to maintain architectural and mission-critical documentation. Never merge these files into the default branch.