# Mission

  Gitboard provides a local code-change dashboard for GitLab and GitHub. It enables developers to triage CI status, open MRs/PRs, and failed jobs, alongside managing local checkout and worktree states. The system orchestrates local forge CLIs (`gh`, `glab`) and `git` to provide a unified view of development activity.