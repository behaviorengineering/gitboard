# Conventions

  - **Language**: The project is written in Go.
  - **CLI/TUI**: The project uses `process-compose` for TUI-based service management during development.
  - **Configuration**: Configuration is managed via YAML files located in `~/.config/gitboard/`.
  - **Dependency Management**: Uses Go modules (`go.mod`, `go.work`).